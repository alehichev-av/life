<UserControl x:Class="life.Views.LifeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:life.Views"
             xmlns:vm="clr-namespace:life.ViewModels"
             xmlns:conv="clr-namespace:life.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Width="400" Height="400">
    <UserControl.DataContext>
        <vm:LifeViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <conv:BoolToBrushConverter x:Key="BoolToBrushConverter"
                                   AliveBrush="Black"
                                   DeadBrush="White"/>
    </UserControl.Resources>
    <ItemsControl ItemsSource="{Binding Cells}" Width="400" Height="400">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid Rows="{Binding Rows}" Columns="{Binding Columns}"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button Background="{Binding IsAlive, Converter={StaticResource BoolToBrushConverter}}"
                        BorderBrush="Gray"
                        BorderThickness="0.5"
                        Width="20" Height="20"
                        SnapsToDevicePixels="True"
                        Command="{Binding ToggleCommand}"/>

            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
